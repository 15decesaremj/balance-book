# Changelog

## Unreleased

## 2.0.9 - 2026-07-27

- Replaced repeated per-day revolving-debt reconstruction with an exact change-point projection
  that continues to delegate every balance change to the canonical single-date debt model.
- Reused date-invariant loan, asset, and refinance inputs across expected and conservative
  net-worth series without changing either forecast mode's cash, receivable, or debt values.
- Added revision-aware forecast snapshot reuse across Overview, Forecast, notifications, and other
  route mounts. Financial writes invalidate the cache before downstream refreshes, while
  presentation-only notification and appearance changes no longer force a full forecast rebuild.
- Reduced the copied owner-profile forecast build from about 22.4 seconds to about 3.5 seconds and
  eliminated duplicate authenticated builds; cached route switches no longer rebuild the forecast.
- Added canonical-versus-optimized debt-series parity coverage plus cache isolation, horizon,
  financial-revision, date-boundary, and explicit-clear regression coverage.
- Advanced React Router to 8.3.0 and forced vulnerable transitive `brace-expansion` versions to
  5.0.8 after the release audit identified newly disclosed production advisories. A narrow tracked
  compatibility patch preserves the legacy CommonJS function export required by ExcelJS's older
  minimatch paths while retaining 5.0.8's bounded expansion implementation.

## 2.0.8 - 2026-07-24

- Added a separate Microsoft Store MSIX channel with exact Partner Center identity inputs,
  deterministic `X.Y.Z.0` package versions, Store-managed updates, and no competing GitHub updater.
- Added a copy-first, hash-verified, recovery-journaled migration from the direct profile into
  package-private Store data while preserving the original profile and recovery backups.
- Added explicit local-financial-data consent before onboarding collects financial information,
  plus a public privacy policy and Store-aware update controls.
- Added Store listing/compliance materials, local package validation, and a protected manually
  dispatched Microsoft Store workflow using pinned official Microsoft tooling.

## 2.0.7 - 2026-07-23

- Corrected **Net monthly free cash flow** to scan a clean future year and use its weakest rolling three-month average. This preserves known scheduled card payments while preventing extra-paycheck months and later loan payoffs from inflating the current recurring budget margin.
- Classified explicit scheduled card-payment records in the selected run, so Cash Forecast separately shows the included monthly payment effect and the recurring margin before those payments.
- Verified the corrected native owner-profile result against the private reference check without adding any owner values to committed fixtures or documentation.
- Corrected the public-history audit to accept normal divergent automation and pull-request refs only after they pass the same isolated one-root, complete-history privacy, and private-commit-intersection checks. The required main branch remains the exact reviewed source, and public tags must remain inside its history. Exact public GitHub automation identities are recognized only in commit/tag metadata; arbitrary emails remain blocked.

## 2.0.6 - 2026-07-23

- Added a short first-run “Which parts fit your finances?” step. Income, bills, cards, loans, money owed, and assets can each be marked applicable or not; skipped setup steps and everyday navigation then match those answers.
- Kept hiding presentation-only: existing records, history, backups, and forecast effects remain intact. Searchable Settings can reopen the exact **Visible sections** control and re-enable any tool at any time.
- Added a compact, persistent desktop navigation mode with a fixed circular collapse/expand control, a matching logo toggle, subtle region separators, and a searchable Settings preference. The native File/Edit/View/Window menu now stays out of the way until the pointer reaches the top edge, while retaining the standard Alt-key fallback.
- Corrected **Next statement position** to anchor one cycle after the purchase-bearing **Available this cycle** statement, then take the lowest total position from that following due date forward. The purchase adviser now uses the same forward-low definition.
- Added signed-public-build update foundations with Beta and Stable channels, delayed and deduplicated checks, offline-safe status, defer/restart controls, notification-center integration, unsaved-form protection, a verified pre-update SQLite recovery snapshot, and a one-time post-update confirmation.
- Kept owner-test builds network-quiet by compile-time disabling the update client unless the signed public release workflow explicitly enables it.
- Added fail-closed handling for databases created by a newer Balance Book version. The application now refuses the file without applying pragmas, migrations, or backups and closes the failed database handle cleanly.
- Added protected GitHub workflows for a signed immutable beta, a rollback-only update feed, provenance attestations, and a GitHub Pages feed. Public binary publication remains blocked until a trusted Windows Authenticode certificate is configured and the release lifecycle gates pass.
- Added a long-run **Net monthly free cash flow** card to Cash Forecast. It measures the average change in cash plus Money Owed over a clean future 12-month window, after current-cycle noise has cleared, and discloses the effect of known scheduled card payments.
- Added carried-balance interest estimates to Credit Cards with explicit ordinary and whole-balance promotional APRs. No carried balance shows `$0.00`; a carried balance without an APR shows **Not available**; promo 0% correctly shows `$0.00`.
- Kept interest forecasting experimental and off by default. Forecast interest is added only when both the profile-wide setting and that card's opt-in are enabled, and never rewrites a locked statement.
- Added recovery-safe schema 36 for the per-card interest controls, with legacy-safe defaults that leave existing forecasts unchanged.
- Passed the complete 2.0.6 source gate: formatting, lint, strict type checking, native SQLite rebuild, 726 tests across 81 files, the 219-file privacy scan, production packaging, and the packaged Electron journey with every authenticated route audited at eight widths from 1440px through 320px.

## 2.0.5 - 2026-07-23

- Added **Bills & subscriptions** under Accounts, backed by the existing recurring cash-event model. Users can edit names, amounts, next dates, cadence, end dates, certainty, payment accounts, and active status without creating a parallel ledger.
- Added explicit card-bill treatment: a bill defaults to already included in the entered card total, or can be set to add a new card charge on each billing date. Reimbursable and 50/50 shared options add linked Money Owed on each occurrence without prematurely moving bank cash.
- Added projected net worth as the fifth Cash Forecast chart series. Investment assets now support an expected annual return, fixed monthly contributions, gross-pay-based employee contributions, and employer match assumptions while preserving take-home cash as entered.
- Added recovery-safe schema 35 for the new investment assumptions, including interrupted-marker reapplication without overwriting stored values.
- Rebuilt the financial center around one fixed-size three-way surface. Notices, Bills, and Balances retain the same geometry, use a sliding segmented control and directional content motion, keep unread dots fully inside cards, and wrap right-side labels instead of clipping them.
- Corrected the Setup level-heading layout and extended the route/viewport audit to Bills & subscriptions. Editors now reveal consistently, duplicate bill saves are synchronously locked, and motion/reveal helpers degrade safely when optional browser APIs are unavailable.
- Reworked customer-facing language across high-traffic pages and settings: Cash Forecast, Recurring Plan, Credit Cards, Money Owed, net-worth totals, balance checks, protected minimums, and safety settings now use shorter consumer-facing terms while preserving precise explanations where needed.
- Passed the complete 2.0.5 source gate: formatting, lint, strict type checking, native SQLite rebuild, 691 tests across 72 files, the 215-file privacy scan, production packaging, and the packaged Electron journey with every authenticated route audited at eight widths from 1440px through 320px.
- Built and installed the exact unsigned 2.0.5 owner candidate from clean product commit `682686005f6f5bc223c73be71d2ef7e5c3b5b4fd`. The guarded upgrade preserved the owner credential and financial fingerprints and all 152 managed records while schema 35 applied; the requested 401(k) assumptions were then saved as one separate audited edit.

## 2.0.4 - 2026-07-23

- Restored the complete Fluent theme boundary around the document-level financial-center portal so its intended spacing, typography, glass surfaces, rounded notice cards, bill cards, and balance cards render correctly in both dark and light modes.
- Refined Notices with contained headings, readable explanations, resilient wrapping, stronger card depth, and responsive spacing while retaining the compact number/X header control.
- Advanced the notification presentation fingerprint once so conditions acknowledged under the broken presentation re-alert in yellow with their unread count; **Mark all read** then persists the normal quiet grey zero state.
- Added unit and packaged regressions for the theme boundary, one-time presentation-contract advance, unread-to-zero behavior, computed panel padding, rounded card geometry, textured card backgrounds, phone containment, and all three financial-center views.
- Corrected the all-in-one release verifier to use the same single-worker process pool as the normal test command, eliminating the native SQLite access violation caused by its stale worker-thread override.
- Passed the complete 2.0.4 source gate: formatting, lint, strict type checking, native SQLite rebuild, 679 tests across 69 files, the final 215-file staged privacy scan, production packaging, and the isolated packaged Electron journey.
- Built and installed the exact unsigned 2.0.4 owner candidate from clean product commit `009081942f5877352a94eba4e4db7dc998454ca7`. Setup SHA-256 is `61E6FF9FA62948A18CC57672CF7D82ED5AE13B469D66D37C62F3D7EFF1A03DB1`; the installed executable is byte-identical to packaged SHA-256 `CF76C302E15BA34B2EC6F3B9D591B0045DC8660469636B8E90E5697D2F227D40`. The pre-install, post-install, and post-launch schema-34 database snapshots are byte-identical, and the credential and financial fingerprints, all 152 managed records, and SQLite integrity `ok` were preserved.

## 2.0.3 - 2026-07-23

- Matched the header notification circle to the Log out control's exact height and made the same trigger become a circular close control while the financial center is open, preserving identical screen coordinates for one-click open/close.
- Changed the header notification number to unread state: **Mark all read** now changes the control to its quiet grey treatment and zero without resolving or deleting underlying financial conditions.
- Removed redundant counts from the Bills and Balances tabs while retaining the useful Notices attention count.
- Replaced the separate in-app brand artwork with the exact canonical open-book-and-trend SVG used by the application icon family, with explicit centered sizing for compact sidebar and sign-in placements.
- Added an atomic **Log an expense** action beside Overview purchase advice. Cash expenses reduce the selected account; card expenses become current-cycle activity without prematurely moving cash; and optional reimbursable or shared treatment creates exactly 100% or 50% Money Owed through the existing receivable model.
- Hardened the Overview expense contract, ownership/date/source validation, audit lineage, odd-cent shared splits, focus return, failure behavior, and rollback semantics with database, contract, renderer, and packaged end-to-end coverage.
- Isolated the native SQLite test suite in single-worker child processes on Windows after diagnosing an intermittent worker-thread access violation; all 677 tests across 69 files now complete deterministically.
- Passed the complete 2.0.3 source gate: formatting, lint, strict type checking, native SQLite rebuild, 677 tests across 69 files, the final 214-file staged privacy scan, production packaging, and the isolated packaged Electron journey.
- Built and installed the exact unsigned 2.0.3 owner candidate from clean product commit `9889b8d41288871bb69cc90b805c34e49300281c`. Setup SHA-256 is `7A3BF3BAF6BCB43EC2149DB2DFAF1B8806103B776A24A6481803082C81C62CA9`; the installed executable is byte-identical to packaged SHA-256 `49DD2CB865414847AEB64AB5F1CB6BACB43B1C8F45D3571F7630B05DA43B4A5A`. The pre-install, post-install, and post-launch schema-34 database snapshots are byte-identical, and the credential and financial fingerprints, all 152 managed records, and SQLite integrity `ok` were preserved.

## 2.0.2 - 2026-07-23

- Corrected each Overview card's next-statement position to use the lowest total cash-plus-open-receivables position on or after the following contractual due date, rather than only the balance on that date.
- Added named Money Owed accrual events to Forecast so recurring shared-expense entries retain their source and purpose without entering a bank account before receipt.
- Added an atomic **Apply automatically** receipt path on Money Owed. One bank deposit is allocated oldest-first across open balances and recurring occurrences, retained as occurrence-specific audit lineage, and rejected without mutation when the receipt exceeds the available owed balance.
- Removed the presentation-only import-differences notification while preserving the reconciliation/import review tools and their canonical records.
- Audited the owner's cited following-statement example against the current workbook and live forecast. The workbook's lower value delays the early SoFi paycheck allocation by moving it out of PNC before payday; Balance Book preserves the real two-day-early SoFi income and later PNC remainder rather than copying that timing defect.
- Passed the complete 2.0.2 source gate: formatting, lint, strict type checking, native SQLite rebuild, 672 tests across 68 files, the final 214-file staged privacy scan, production packaging, and the isolated packaged Electron journey.
- Built and installed the exact unsigned 2.0.2 owner candidate from clean product commit `16d63c6affce3919f7c7bb147be1afb75768b026`. Setup SHA-256 is `B889962578723778CC5560FFB0594CC168B92D9CF4F8D66B01C9B848C0F197B1`; the installed executable is byte-identical to packaged SHA-256 `357D5203F45BD617CE006605AE7D797479114EDAE3CC31B93927AB9821A59FA9`. The pre-install and post-launch schema-34 database snapshots are byte-identical, and the credential and financial fingerprints, all 152 managed records, and SQLite integrity `ok` were preserved.

## 2.0.1 - 2026-07-22

- Rebuilt the header attention control as a compact number-only indicator and one responsive financial center with keyboard-accessible Notices, Bills, and Balances views.
- Made funding warnings lead with the exact projected cash shortfall, account minimum, deeper same-episode gap, and separately labeled Money Owed coverage.
- Added a canonical upcoming-bills model over the complete expected daily ledger, with compact scheduled-payment summaries that exclude income, hypothetical activity, completed payments, and internal transfers.
- Added a current-balance glance over the existing calculated account and revolving-debt read models, including cash minimums, source freshness, current card balances, statements, available credit, and contractual due dates without maintaining a second ledger.
- Refined the shell with a purpose-built vector Balance Book mark, consistent vector navigation iconography, subtle textured glass surfaces, ambient financial motion, and complete reduced-motion behavior.
- Passed the complete 2.0.1 source gate: formatting, lint, strict type checking, native SQLite rebuild, 669 tests across 67 files, the 212-file staged privacy scan, production packaging, and the isolated packaged Electron journey, including Notices/Bills/Balances switching and phone-width geometry.
- Built and installed the exact unsigned 2.0.1 owner candidate from clean product commit `50dde3ef0d0e899afee1f776adc8f2b0a2977639`. Setup SHA-256 is `B641D0F8123FE98954207DB706D50DFDECD42571E8C18F25A61B3FCA40B4E52F`; the installed executable is byte-identical to packaged SHA-256 `1DE281937CF0F8B4973096303E8683885E11EF54E302AAF9172791F5E2BE8B5B`. The durable live database remained unchanged through installation before first launch, and the post-launch schema-34 snapshot retained the credential and financial fingerprints, all 145 managed records, and all 2,475 rows across 18 canonical tables with SQLite integrity `ok`.

## 2.0.0 - 2026-07-22

- Added a deterministic financial notification center with separate Needs action and Updates sections, a badge that counts unresolved financial conditions rather than unread messages, stable condition fingerprints, read-versus-resolved behavior, responsive desktop/mobile presentation, and exact canonical inline actions for balance confirmation, statement-payment confirmation, expected-event completion, and Money Owed release.
- Added recovery-safe schema 34 for profile-scoped notification presentation metadata. Read, snooze, and dismissal metadata remain device-local and intentionally stay out of portable financial backups; the underlying conditions regenerate from canonical records after restore.
- Separated authoritative source balances and dates from calculated-through-today positions and post-source activity on Overview. The existing account/card quick editor now provides Now, Activity, and Plan areas with source snapshots, recent records, audit history, scheduled actions, and compensating-reversal Undo without creating a second ledger.
- Shortened Overview around card Spending Power, current account positions, funding risk, important events, and the purchase adviser. Supporting card lows remain disclosed, while purchase comparisons now explain resulting available spend, settlement dependency, and following-statement position for every safe and unsafe option.
- Evolved Financial Records into Activity & records: a filterable timeline over existing balances, cash/card activity, statements, payments, income, bills, receivables, loans, assets, reconciliations, imports, reversals, and audit envelopes. Daily forecast events now trace to the exact canonical source editor.
- Reorganized established onboarding into Start, Improve accuracy, and collapsed Advanced levels while preserving every existing setup field and advanced route.
- Hardened new actions with immediate duplicate-submit locks, profile-scoped reads, recovery-safe migrations, exact-cent mutation helpers, deep-link focus, keyboard focus return, and route/viewport coverage down to 320px.
- Passed the complete 2.0.0 source gate: formatting, lint, strict type checking, native SQLite rebuild, 664 tests across 66 files, child-process backup verification, the 209-file privacy scan, production packaging, and the isolated packaged Electron journey. No required source gate was skipped.
- Generalized the guarded Windows release and validation tools to derive `V2` handoff names from version 2.0.0 while preserving the existing `balance_book_mvp` installer identity and compatibility with prior V1 artifacts.
- Built and installed the exact unsigned 2.0.0 owner candidate from clean product commit `70da5f98ad473ba8a754d0a1da4de2d749a92fdb`. Setup SHA-256 is `9009902A6AA264A2734002549959153940D1C3EC23CF73EB3B4E1500874F4F10`; the installed executable is byte-identical to packaged SHA-256 `B4BA46BE3743125DCCC4714A156F6C4D5F997E7F553AEBA743D40FF91B627462`. The durable live database remained unchanged through installation before first launch, and the schema-34 post-launch snapshot retained both profiles, matching credential/financial fingerprints, all 145 managed records, and all 2,475 rows across 18 canonical tables with SQLite integrity `ok`. Signing, fresh-profile lifecycle, and a fresh private-backup handoff remain open public-release gates.

## 1.1 owner-candidate history

- Advanced the owner-candidate identity to 1.1.10 so the refined Overview card facts and hub typography cannot be confused with the installed 1.1.9 bytes.
- Kept the Overview Still owed fact only for cards with actual carrying-balance evidence, and made that fact report the carried amount instead of repeating a normal coming-due statement. Projected lows now show open money owed at the exact limiting total-position date beside the total and account lows.
- Stacked every Accounts and Planning hub title above its description with shared block semantics, resilient wrapping, explicit spacing, and route/viewport collision coverage.
- Passed the complete 1.1.10 source gate: formatting, lint, strict type checking, 645 tests across 60 files, child-process backup verification, the 196-file privacy scan, production packaging, and the isolated Electron journey. The journey proves both carried and non-carried Overview card states, receivables at every limiting card low, stacked hub typography, accessibility, persistence, and the full authenticated route/viewport matrix.
- Built and installed the exact unsigned 1.1.10 owner candidate from clean product commit `f601d8b3cfeb84e98188b6ece5427208fc29057e`. Setup SHA-256 is `D2D946B66B3F14A4E1FF5EBFBD28956307D8C44CF061C3415CF7472334966FB7`; the installed executable is byte-identical to packaged SHA-256 `A1BA5B3D9561080551F893996100613C70DCEDFFA0EC5B47CBE4C0793228D9CE`. The durable live database and WAL were unchanged through installation before first launch, and the post-launch schema-33 snapshot matched the pre-install credential fingerprint, financial fingerprint, and all 145 managed records with SQLite integrity `ok`. The verified application is open for owner testing; signing, fresh-profile lifecycle, and a fresh private-backup handoff remain open public-release gates.
- Advanced the owner-candidate identity to 1.1.9 so the streamlined information architecture, quick transactions, and same-day balance boundary cannot be confused with the installed 1.1.8 bytes.
- Reorganized the application around five stable destinations—Overview, Forecast, Accounts, Planning, and Settings—while preserving every advanced route through concise section navigation and focused Accounts and Planning hubs. Cards now use compact summaries with an explicit management workspace, Settings uses progressive categories, Loans keeps refinance/history at portfolio level, and Refinance guides the same complete input set through three steps with a persistent summary.
- Made each Overview cash-account and card summary a keyboard-accessible quick-update surface with a compact Balance/Transaction dialog. Cash deposits and withdrawals, card purchases and credits, current issuer balances, and latest statement corrections all write through canonical audited records, preview their direct effect, refresh downstream forecasts and Spending Power once, retain failures in place, and preserve links to full settings.
- Added recovery-safe schema 33 for the explicit same-day post-snapshot boundary. A transaction entered on an account or card balance-as-of date now applies after that reported balance without changing legacy events; card credits remain signed activity and reduce revolving debt instead of being clamped away.
- Hardened route-driven Settings categories, phone-width category layout, focus return, duplicate-submit locks, generated focus-guard accessibility handling, and the packaged route/viewport collision audit. The stateful Electron journey covers the complete redesigned workflow against a temporary synthetic profile without mutating live financial data.
- Built and installed the exact unsigned 1.1.9 owner candidate from clean product commit `b219d81018b27668a5643e50ea89a29a0e0fe3fd`. Setup SHA-256 is `FCECE14B156A4748851C25B1E3E9218FD6178867BA6522CB7F35668628175AC2`; the installed executable matched packaged SHA-256 `85909FFB53A2A2D9D65CE080F74EEA7D8CA2B57A058D978B7B890D74957D9FC0`. The durable database and WAL were unchanged through installation before first launch, schema 33 applied successfully, and an independent post-launch snapshot matched the pre-install credential fingerprint, financial fingerprint, and all 145 managed records with SQLite integrity `ok`. The verified app is open for owner testing; signing, fresh-profile lifecycle, and complete private-backup handoff remain open public-release gates.
- Advanced the owner-candidate identity to 1.1.8 so the new Overview card-balance controls cannot be confused with the installed 1.1.7 bytes.
- Added compact Overview edits for each card's issuer-reported current total and latest locked statement. Current-total saves are dated at the current financial point without rewriting statement cycles, while statement corrections preserve payment evidence and show a restrained warning only after the recorded due date.
- Reduced the visible checking-account action to a small pill-style Edit control and extended the synthetic Electron journey across Overview, Cards, and the persisted record graph to prove that card edits preserve checking balances, card terms, and scheduled payments.
- Advanced the owner-candidate identity to 1.1.7 so the corrected delinquency semantics and prominent Overview balance controls cannot be confused with the installed 1.1.6 bytes.
- Separated revolving carry from delinquency: a recorded partial or special-financing payment can retain statement carry without being labeled past due, while a genuinely unpaid statement after its due date remains overdue.
- Moved the existing audited cash-account balance editor into a prominent Overview section immediately below the financial status. Each visible checking account now has an explicit **Update balance** action that defaults to the current financial date and refreshes the native forecast, account lows, funding guidance, and card runway through the existing persistence path.
- Advanced the owner-candidate identity to 1.1.6 so the new card-runway and next-statement surfaces cannot be confused with the installed 1.1.5 bytes.
- Added an Overview card sorter that defaults to the shortest remaining statement period and supports reverse period, name, available-spend, current-balance, and last-statement orders while keeping unavailable values last.
- Added each card's total cash-plus-open-receivables position on the first due date after its next due date. Manual-payment cards now receive a usable runway only when an explicit current-cycle paydown is scheduled, while remaining excluded from hypothetical full-statement purchase advice.
- Advanced the owner-candidate identity to 1.1.5 so the Overview balance editor cannot be confused with the installed 1.1.4 bytes.
- Added a compact balance editor to every visible Overview cash-account tile. It defaults to today's financial date, preserves the account's thresholds and operating settings, writes through the audited cash-account record path, and immediately refreshes totals, account lows, funding actions, purchase guidance, and card spending power.
- Advanced the owner-candidate identity to 1.1.4 so the control hardening and schema-32 payment-account migration cannot be confused with the installed 1.1.3 bytes.
- Audited every application route and hardened the editable surfaces for cards, statements, scheduled card payments, recorded payments, loans, income, cash events, receivables, assets, reconciliation, settings, setup, data tools, and saved scenarios. Edit actions now reveal and focus their editor consistently, repeat clicks re-reveal an already open editor, async controls close same-tick duplicate-submit windows, and failures stay with the surface that caused them.
- Made card payment evidence account-specific. A recorded statement payment now stores the cash account actually used, schema 32 preserves that fact across restart and portable backup, and the forecast emits the cash release once from that account while retaining legacy fallback behavior.
- Preserved status, notes, recurrence, and actual-payment evidence when scheduled card payments are edited; made recurring-series cancellation scope explicit; kept stale planned payments correctable; and clarified that promotion dates are informational until an interest model exists.
- Made installment-loan edits atomic across debt and cash. Funding-account changes move only future linked drafts, recurring instructions split without rewriting history, incompatible schedule changes roll back, and inactive, excluded, or cancelled-refinance loans cannot leak future cash outflows.
- Prevented Loans and statement cycles from being deleted while linked payment history or instructions remain. Specialized debt-event linkage is cleared when an event is intentionally converted to an ordinary cash record, while unrelated import lineage is preserved.
- Hardened purchase evaluation, setup-checklist decisions, JSON import, account and policy settings, reconciliation status edits, and every saved-scenario action against stale responses or cross-surface interference. Persisted responses repopulate the editor and all forecast-dependent views instead of leaving stale local state.
- Expanded the isolated desktop journey to verify editor visibility, focus, viewport placement, repeat edits, save-and-reopen persistence, loan funding-account propagation, and single-effect behavior under rapid actions. The current sandbox-safe broad run passes 602 tests across 58 files with 17 explicitly skipped cases; the two child-process backup-verifier cases passed earlier in the same implementation turn, but a fresh combined/package run is blocked until the local cache can retrieve two pinned Electron Forge archives.
- Advanced the owner-candidate identity to 1.1.3 so the new card-balance surface, profile preferences, portable-profile format, and schema-31 migration cannot be confused with the installed 1.1.2 bytes.
- Rebuilt each Overview card around four distinct answers: available spend for the current cycle, current posted balance, latest statement balance, and the statement remainder still owed after confirmed or paid payments. Removed generic setup/runway tags, retained explicit unavailable reasons, and made the three balance facts responsive down to the supported 320px viewport.
- Corrected revolving-debt evidence so a scheduled forecast payment no longer clears actual statement debt merely because its amount and date are locked. Only a confirmed or paid payment, or authoritative cycle payment evidence, reduces the amount still owed; partial payments and newer-statement supersession remain explicit and tested.
- Kept same-day net worth internally consistent when an expected card payment reaches the cash ledger before the user records it as paid: Overview continues to show actual statement debt, while net worth uses only the projected payments applied to that exact expected cash close.
- Moved Theme out of the global header and into a profile-scoped Settings workspace. Added persisted default Overview mode, compact spacing, reduced motion, and show/hide controls for the daily summary, upcoming events, and wider financial picture while keeping card runway, account coverage, and safety warnings visible.
- Added recovery-safe schema 31 and portable profile V3 for experience preferences. V2 backups remain strictly readable and normalize to current defaults; V3 exports and restores carry complete validated preferences without changing destination credentials.
- Hardened the standalone backup verifier for portable V3, committed refinance plans, profiles without a cash policy, saved theme/preferences/onboarding state, restart login, and SQLite integrity. Type-checking now includes release-tool `.mts` sources, and a real encrypted CLI round trip protects the path.
- Reduced persistent prose on the highest-traffic pages, moved financial definitions and calculation detail into accessible disclosures, shortened account-low labels, and retained the warnings and audit context needed to act safely.
- Passed the complete 1.1.3 source gate: 573 synthetic tests across 53 files, formatting, lint, strict type-checking, the 188-file privacy scan, production packaging, the isolated Electron input-to-output journey, serious/critical accessibility checks, saved preference behavior, and the 120-state route/viewport collision matrix.
- Built and installed the exact unsigned 1.1.3 owner candidate from clean commit `8ce23bb018154f4b5e16a20833264df7370bf4ff`. Setup SHA-256 is `EE52DA325061B4DD9F203777D892E13AD45B101CA160DFDA75B361FCE23E7D7E`; the installed executable matched packaged SHA-256 `F1019521C7916C3036175748DF382FE7DFC1EE2DC899569483570F4031363DFF`. The installer left the live database untouched before first launch, schema 31 applied with a pre-migration-v30 safety backup, SQLite integrity remained `ok`, and all 18 pre-existing data tables retained every prior column value and row.
- Advanced the release-candidate identity to 1.1.2 so the responsive shell, account-visibility preference, contractual card due dates, and schema-30 migration cannot be confused with the previously installed 1.1.1 bytes.
- Rebuilt Funding Actions into an explicit three-region responsive grid, moved the desktop-to-mobile shell transition to the width where the sidebar no longer leaves a usable content column, and constrained every Fluent field/control to its grid track. Reusable cards, headings, actions, and form grids now wrap long user-entered names cleanly down to 320px.
- Added a persistent per-account "Show on Overview" setting. It hides only the selected cash-account tile; the account remains in the native forecast, protected-minimum calculation, card runway, and funding guidance. Older databases and portable backups default every account to visible through recovery-safe schema migration 30.
- Added each card's earliest unresolved contractual due date to Overview without substituting its modeled cash-payment date. Past, today, future, and unavailable states are explicit, and stale statement-close dates no longer masquerade as "Resets today."
- Added a packaged route-wide visual invariant audit across all 15 authenticated pages at eight widths from 1440px through 320px. It rejects page overflow, overlapping controls, and watched-header/Funding-Action collisions while respecting content that is genuinely hidden or clipped inside disclosures and scroll regions.
- Built and installed the exact unsigned 1.1.2 owner candidate from clean commit `dcb1177c5f69d2407ca90f0a1f3345e8ef0984d4`. The installed executable matched the packaged SHA-256, the live database and an independent SQLite backup remained byte-identical across installation before first launch, schema 30 applied on launch, and post-upgrade SQLite integrity remained `ok`.
- Advanced the release-candidate identity to 1.1.1 so the next installer, uninstall helper, portable backup metadata, and public mirror are versioned separately from the previously built 1.1.0 artifact.
- Reworked the complete visual system around a dark-default, Apple-inspired liquid-glass shell with rounded cards and pill controls, cleaner spacing, calmer typography, theme-aware surfaces, a single blue input focus ring, accessible system-blue contrast, and phone layouts with no horizontal overflow.
- Added statement-reset countdown badges, exact first/deepest account-funding episodes, sequential Money Owed coverage that cannot be promised twice, an expandable Upcoming Cash Events list, and separate cash-versus-card purchase answers. Current-cycle account diagnostics now stop at the later of the limiting total-position date or actual payment date so unrelated later episodes remain in Account coverage.
- Added card portfolio summaries, known-limit utilization, editable future payments, policy-driven full/minimum/fixed/manual autopay remainders, recorded payment release, actual underpayment carry, overpayment credit, and status-aware deduplication between planned and paid cash evidence.
- Split Money Owed into current Settle Up balances and concise recurring future receivables. Future installments accrue only on their occurrence dates and enter a selected bank account only when explicitly released, including partial receipts with durable audit lineage.
- Added a two-year Charts workspace driven by recorded history and the native forecast/debt engines, plus payoff-oriented Loans, compact Reconciliation facts with collapsed rare tools, and a clearer checklist-style setup experience.
- Rebuilt Overview card runway around the current cycle's due date, the due-date-forward total-position low, and an independently minimized end-of-day low for every included checking account. A negative account low remains visible without relabeling or suppressing positive total available spend, and Expected mode no longer promotes liquid-cash or transfer warnings into the primary answer.
- Prevented a zero-balance recurring receivable from creating planned cash before its first accrual. Later planned receipts map to the accrued occurrence, while real recorded early or late receipts remain attached across rolling forecast boundaries and static opening balances retain their own settlement behavior.
- Reconciled a later dated profile refresh entirely through editable account, card-cycle, and Money Owed records, then compared every available daily reference row and every card runway without replaying workbook formulas. Intentional differences remain limited to explicit release-only receivables staying in Money Owed until receipt, documented timing corrections, correct receivable reduction, and integer-cent normalization of fractional-cent source values.
- Made an assigned Money Owed date visibly account-specific: expected cash enters only the selected account on that date, while a currently owed static balance falls by the same amount. Recurring contributions now show their cash and accrued Money Owed effects separately when those amounts differ. An unconfirmed date remains expected-only even when the balance itself is confirmed, so it cannot improve the conservative forecast prematurely.
- Added schema 29 to repair only untouched imported static receivables whose source supplied no receipt date. The migration keeps the date unconfirmed, uses the first of the following month as an editable expected date, enables the expected cash path, preserves user-edited and historical rows, marks import lineage, writes an audit event, and takes the normal pre-migration snapshot.
- Repaired a legacy future-paycheck shape that moved an early account allocation through an internal transfer instead of recording it as employer income two days before the official payday. The migration preserves audit history, keeps consolidated cash timing truthful, and the routed-income editor now names the official-payday input consistently for sighted and assistive users.
- Defined the repeatable **Published Status** workflow: clean private development state, a sanitized clean-history public mirror, software-only signed release assets, independent post-download verification, reversible blank-onboarding testing, and a final File Explorer plus uninstall/install handoff.
- Added initial-build and later-sync clean-history mirror tooling, a signed seven-file public-release assembler, and a fail-closed publication checker that binds repository trees, release tags, downloaded artifacts, packaged/installed executables, hashes, versions, and publisher identity.
- Clarified that a fresh onboarding simulation renames retained app data instead of deleting it, and documented a no-merge restoration path.

- Added a flexible installment-loan setup solver that accepts partial lender facts, labels every calculated field, derives payment, APR, balance, origin, term, maturity, or payoff when the facts constrain a solution, and refuses contradictory or underdetermined setups instead of silently guessing.
- Added exact monthly and biweekly dated amortization, fully amortizing and explicit balloon/bullet structures, separate total cash draft and debt-service amounts, and a visible principal/interest/remaining-balance schedule with contractual maturity distinct from modeled payoff.
- Made loan-payment records explicit: a regular-draft override reconciles cash without duplicating the generated payment, while additional principal reduces the liability on its exact date, caps at outstanding principal, accelerates payoff, lowers future interest, and leaves normal drafts in place.
- Added one authoritative per-card revolving-debt snapshot across Cards, Overview, and Net Worth: latest statement, amount currently due, open-cycle activity, total current balance, available credit, and balance carrying remain distinct. Paid-in-full cards carry zero without erasing statement balances or history, and partial or overdue payments remain visible.
- Added effective-dated card and line-of-credit retirement/reactivation. The closure date is the first blocked purchase date; existing debt, the final earlier cycle, historical statements, and payoff cash remain active while new purchases, baselines, cycles, Spending Power, and advisor eligibility stop.
- Stopped future-cycle generation from inventing an already-past synthetic statement when onboarding history is absent, while retaining same-day obligations and real stored overdue statements.
- Reconciled Overview and per-card Spending Power to one native total-position runway: the minimum bank-cash-plus-money-owed position from each card's current-cycle due date forward, less the global protected threshold. The dashboard now defaults to Expected, shows independently dated account lows plus a separate payment-date funding snapshot, and keeps cash-only and negative-account warnings visible without replacing the runway answer.
- Fixed production packaging so SQLite build inputs are staged before Forge's Electron-ABI rebuild, then the runtime-only staging hook preserves that rebuilt binary instead of overwriting it with the development Node-ABI copy.
- Preserved paycheck destination editor order independently from per-account early-arrival timing, so reopening a routed paycheck keeps every numbered destination, allocation rule, and amount attached to the row the user originally entered.
- Added atomic multi-account paycheck routing around one official payday and total take-home, with fixed destination amounts, one calculated remainder, account-specific calendar-day early arrivals, and an inspectable upcoming-deposit preview.
- Made the cash forecast expand the official pay schedule before applying each account's arrival timing. Only destination legs enter cash, so no aggregate paycheck, duplicate inflow, or transfer-in-transit artifact is created.
- Reworked raise planning to use whole-paycheck take-home and an explicit destination for the increase, while optional bonuses require their own destination and date. Payroll deductions already included in take-home are never subtracted again.
- Kept routed paychecks together across create, edit, and delete operations. Financial Records identifies grouped legs and directs edits to Income and raises instead of allowing one allocation to drift from its plan.
- Hardened paycheck schedules against omitted semimonthly starts, duplicate plan or allocation identities, and stale linked raises. Base-pay timing edits now cascade cadence, end date, and matching account arrival timing to a linked raise atomically, or reject the edit if its effective payday would no longer exist.
- Added dated Money Owed receipts with an explicit deposit account, a first-of-next-month entry default, and mutually exclusive one-time, fixed-recurring, or bill-relative timing. Bill-relative receipts follow the real recurring bill date across short months and replace recorded actual receipts exactly once.
- Passed 550 synthetic unit and integration tests across 51 files for the current intended public 1.1.1 source, plus formatting, lint, strict type-checking, the 185-file privacy gate, production packaging, serious/critical accessibility checks, responsive screenshot review, and the complete isolated Electron input-to-output journey.
- Built and installed an offline, explicitly unsigned Windows x64 Squirrel 1.1.1 candidate and passed the packaged-application smoke check. The full isolated Playwright journey passes, including routed paycheck timing, responsive layout, both themes, accessibility, restart, and input-to-output propagation; the owner-profile upgrade preserved the live database byte-for-byte before first launch and installed versioned desktop and Start-menu shortcuts. Signing and fresh-disposable-profile artifact lifecycle gates remain before public release.
- Hardened release metadata so Squirrel `RELEASES` evidence is serialized as a bounded string instead of inheriting PowerShell file-provider metadata, with regression coverage across the build, validation, offline-Squirrel, and public-assembly scripts.

## 1.0.0 - 2026-07-15

- Promoted the local-first financial operations application to the V1 release line with exact version pinning, dark-first daily-driver workflows, editable inputs, per-card safe-spending guidance, account-level low points, and timing-aware funding actions.
- Upgraded portable recovery to the authenticated V2 format: AES-256-GCM encryption with scrypt, strict envelope and record-graph validation, atomic writes and restore, destination credential preservation, and full-fidelity audit/import lineage.
- Added a repeatable Windows x64 Squirrel release workflow, stable upgrade identity, native uninstall helper, exact three-file private handoff validation, installed/package hash and signature checks, and a clearly separated local unsigned-candidate lane.
- Hardened Electron navigation, permissions, IPC sender validation, renderer isolation, Content Security Policy, native-module packaging, and release privacy checks.
- Added the MIT license, third-party notices, contributor and support guidance, security and threat-model documentation, issue and pull-request templates, Dependabot configuration, signed-release gates, and a clean-history public-mirror requirement.
- Expanded the deterministic suite to 172 passing engine, domain, database, migration, backup, renderer-contract, and control assertions, including adversarial encrypted-backup cases and credential-preserving cross-profile restore.

## 0.6.0 - 2026-07-15

- Rebuilt the visual system as a dark-first, premium financial workspace with a calmer hierarchy, rounded surfaces, accessible brand and status colors, responsive page selection, larger controls, and a phone-native daily ledger. Both explicit themes pass the serious/critical axe gate.
- Made Overview answer “How much can I safely spend on each card?” directly. Every card now shows its cash-backed amount, true cash-payment date, current-cycle activity, future total-position and liquid-cash lows, funding-account low and date, protected minimum, and any earlier funding dependency; the purchase advisor still verifies exact amounts across every eligible card.
- Made card timing truthful at lifecycle edges: deliberately manual cards no longer receive invented dates or setup nags, past-due statements with a future scheduled payment remain visible and funded, and a locked overdue statement without payment timing makes capacity explicitly indeterminate instead of disappearing.
- Expanded Cards into an editable statement operating record with coming-due balance, open-cycle spending, next modeled statement, complete past-month history, baseline handoff, and incomplete/manual timing guidance. Optional first-load import also preserves timing-incomplete manual cards and their explicit dated cash payments without inventing statement rules.
- Reworked recurring money owed into occurrence-specific accrual and receipt ledgers. Early, late, partial, and later-installment payments settle only the selected occurrence; cash received before the following shared expense accrues correctly; future “received” dates are rejected before either cash or the owed asset changes.
- Corrected net worth so current rolled cash and receivables agree with the daily forecast, non-liquid/restricted cash remains an owned asset in contractual and economic net worth, and only liquidity-designated cash enters the liquid position.
- Made every major input reachable through a guided editor while keeping Financial Records as the canonical library. The advanced creator is collapsed by default, setup actions deep-link to filtered record types, and income/raise/bonus, bills, card cycles, loans/refinance, receivables, assets, cash accounts, and guardrails all propagate into their native outputs.
- Re-audited the native engine against reference-ledger fixtures without translating spreadsheet formulas. Intentional differences require an explicit classification, and manual card values plus long-run baseline facts remain source evidence in ignored local reports.
- Hardened recovery and first-load import safety: migration backups now use a WAL-consistent SQLite snapshot, local seed tooling requires an explicit directory and live opt-in, repeated runs are no-ops, user-created or edited records block replacement, and existing credentials are preserved.
- Expanded the committed suite to 164 engine, domain, database, contract, and control tests plus the full Electron input-to-output journey. Added exact synthetic regressions for card lifecycle boundaries, combined-scenario eligibility, recurring-receivable occurrence identity and settlement edits, future receipts, non-liquid cash, staged imports before policy creation, contextual record routes, mobile account balances, dark/light accessibility, and the WAL backup defect.

## 0.5.0 - 2026-07-15

- Turned Balance Book into a rolling daily operating view: dated opening balances are replayed through the native expected ledger to establish today, then expected and protected forecasts run forward from the current date instead of repeatedly starting at an old workbook snapshot.
- Added a dedicated Income and raises workspace for typed paychecks, bonuses, commissions, self-employment income, partner contributions, and other income, with one-time, weekly, biweekly, monthly, and semimonthly timing, certainty, destination account, optional end dates, notes, and exact before/after forecast impact.
- Added raise planning from an existing recurring income stream using a new net deposit, an added net amount, or a percentage, plus an effective date, projected or confirmed status, and an optional one-time bonus saved atomically with the raise.
- Made per-account minimums, preferred buffers, transfer lead times, liquidity inclusion, and source-account eligibility first-class controls. The effective global minimum is the larger of the consolidated override and the sum of included account minimums, so account protections automatically populate the portfolio-level guardrail.
- Reworked funding guidance around timing-aware, jointly reserved transfer suggestions. Recurring transfers preserve their debit/credit pairing, delayed transfers remain visible as in-transit cash without reducing consolidated ownership, and Funding Actions show initiation, arrival, amount, and source surplus after all modeled reservations.
- Added a purchase advisor that evaluates every card through its native statement cycle and payment policy, ranks fundable options, shows the exact added card payment and settlement date, includes reward context, and offers a prefilled transfer when a safe source exists.
- Made saved card scenarios retain their card, purchase date, cycle, and settlement semantics when duplicated, combined, or converted into a real commitment.
- Extended missing card cycles from editable card timing terms, carried detailed card activity into the owning cycle once, and made static card Spending Power cash-backed: for full-statement cards it combines available forecast margin with unused statement-estimate headroom while preserving both consolidated and funding-account floors. Non-full-statement or incomplete timing cases are labeled indeterminate instead of implying false capacity.
- Tightened forecast semantics so every active outflow remains in protected cash while only confirmed inflows can fund it; expected and uncertain income states now have truthful controls and messages.
- Expanded synthetic input-to-output coverage for income cadence, raise and bonus certainty, account/global guardrail propagation, delayed and recurring transfers, joint source allocation, card-cycle purchase impact, generated future cycles, and invalid timing/floor combinations. The Electron journey now exercises the same controls, resulting overview/forecast changes, mobile layout, and serious/critical accessibility checks.
- Revalidated ignored local import fixtures against dated reference-ledger rows: account cash, consolidated cash, and receivables reconcile at stored-cent precision with documented integer-cent normalization. Spreadsheet formulas are never runtime mechanics.

## 0.4.0 - 2026-07-14

- Rebuilt Overview around per-card Spending Power, statement obligations, current-cycle spending, future total-position lows, and funding-account shortfalls.
- Added each card's future liquid-cash low alongside its total-position and funding-account lows, so receivables cannot be mistaken for spendable cash.
- Replaced the summary forecast with an auditable daily ledger for total position, liquid cash, money owed, each cash account, and the native events that caused every change.
- Added native recurring receivable accruals and settlements while keeping cash, money owed, and income distinct.
- Added guided complete-field editors for card statements and history, loans, receivables, assets, and investments; retained Financial Records as the canonical advanced library rather than the primary day-to-day interface.
- Added guided cash-account and forecast-event editing to Financial Records, including recurrence, payment method, card assignment, certainty, status, and conservative treatment, while preserving source lineage.
- Added editable protected-floor, preferred-buffer, conservative-receipt, and forecast-horizon guardrails in Settings, and made Baseline's editing routes explicit.
- Prefilled refinance comparisons from active loans and added native payment, total-interest, residual-balance, non-amortizing-quote, closing-cash, funding-account-low, consolidated-low, and safe-to-deploy calculations.
- Rebuilt onboarding as a truthful, resumable progressive setup: only the first cash account and protected floor are required, then a 15-section Assets/Liabilities/Timing/Review flow shows ready, needs-decision, reviewed, and not-applicable states with warnings and a final check.
- Added typed onboarding metadata for available balances, account notes, card issuer/last four, loan lender/type/original and maturity details, investment contributions/match/restrictions/linked liabilities, and shared-expense links/payment instruments.
- Made workbook import review show imported and current values, import and last-change times, forecast impact, related records, warnings, and direct per-value edit actions.
- Labeled every daily forecast event as actual, locked, estimated, hypothetical, or planned and showed whether it appears in both forecast views or expected only.
- Integrated detailed card-funded records into open-cycle spending and its later cash payment exactly once, added an explicit "already included in cycle total" treatment for aggregate baselines, and added Expected/Conservative Spending Power views that use the same native card obligations.
- Made record deletion dependency-safe, honored liquid-asset and active-loan semantics in net worth, and made optional financial fields genuinely clearable.
- Added proper Squirrel install/update/uninstall lifecycle handling so the per-user installer creates and removes application shortcuts without launching a stale setup process.
- Corrected optional workbook import so blank card history remains unknown, active timing terms are authoritative, money owed is itemized, and distinct recurring shared-expense receipts remain separate.
- Expanded the synthetic engine, persistence, contract, installer-lifecycle, and onboarding suite with receivable roll-forward, per-card Spending Power, refinance math, editable guardrails, truthful first-use behavior, and destructive-history protections.

## 0.2.0 - 2026-07-14

- Rebuilt Overview as a calm, decision-first cash plan with stronger hierarchy, active navigation, expected/conservative inspection, a protected-floor status summary, action queue, account lows, an accessible cash path, upcoming cash events, and wider-position context.
- Corrected date presentation so ISO financial dates never shift across time zones and counted underfunded accounts distinctly instead of counting every affected day.
- Reworked optional local import so a workbook supplies only initial data and reconciliation evidence: opening balances come from an explicit as-of row, semantic records are normalized, and expected account paths can be checked at cent precision for a configured horizon.
- Added safe pre-seed database backup, field-level lineage for all imported attributes, user-edit conflict preservation, source checksum verification, and blank-profile isolation checks.

## 0.1.1 - 2026-07-13

- Added the pure financial domain and forecast engine with exact money, timezone-free dates, card cycles, loans, receivables, net worth, scenarios, and 23 synthetic golden cases.
- Added local profile/password access, isolated SQLite persistence, a working setup/forecast/scenario desktop slice, profile themes, and Electron end-to-end accessibility checks.
- Added complete-core desktop sections, resumable onboarding/review, multi-account records and transfers, card terms/cycles, loan payoff/refinance, partial receivable settlements, rewards, assets/net worth, scenario lifecycle/combined evaluation, reconciliation, and explainable overview metrics.
- Added schema migrations through version 5, pre-migration backups, encrypted portable backup/restore, JSON/CSV export, validated JSON import, guarded profile reset, immutable audit evidence, and workbook import lineage/conflict protection.
- Added idempotent ignored local import tooling while preserving blank-profile isolation and leaving credentials unset.
- Added Windows CI, packaging/release-readiness documentation, 39 unit/integration tests, and an expanded both-theme Electron/axe journey.

## 0.1.0 - 2026-07-13

- Established the local-first Electron foundation and repository privacy controls.
- Added a packaged Electron launch-and-interaction smoke test and verified the initial shell visually.
