# Project state

Updated: 2026-07-16

## Current milestone

Balance Book is on the 1.1.0 V1 feature-release candidate line. The application is a local-first Windows x64 financial operations desktop app with editable daily forecasting, per-card spending power, revolving and installment debt tracking, account-level funding guidance, portable encrypted backup/restore, and repeatable Windows packaging.

No signed public binary or published public source repository is claimed. The product baseline at commit `dff8701` passed 425 synthetic unit and integration tests across 41 files, and an offline, explicitly unsigned Windows x64 Squirrel candidate passed the packaged-application smoke check. The current intended public tree passes 463 synthetic tests across 43 files plus type, lint, formatting, script-parser, refusal-path, and independent private-fingerprint checks. Seventeen additional disposable-Git and child-process privacy tests are present but could not run because the restricted environment blocks Node child-process creation. That evidence does not establish public-production readiness: the complete unrestricted Electron journey, signing, clean-history remote, fresh-disposable-profile lifecycle, and publication gates in [PUBLIC_RELEASE_READINESS.md](PUBLIC_RELEASE_READINESS.md) remain open.

## Implemented product foundation

- A deterministic financial engine uses exact integer cents and timezone-free dates for recurrence, expected/protected forecasts, account and consolidated low points, card-cycle settlement, revolving debt, installment amortization, transfer timing, loans, receivables, rewards, net worth, reconciliation, and scenarios.
- The versioned SQLite data layer provides foreign keys, transactions, WAL, migration snapshots, audit events, import lineage, onboarding drafts, and profile-scoped reads and writes.
- Overview answers per-card Spending Power from one reconciled total-position runway, shows every limiting-date and payment-date bank balance plus money owed, and preserves cash-only low points and timed funding actions as separate warnings from the same native ledger.
- Guided pages edit cards and statement history, cash accounts and guardrails, recurring income, routed paychecks with account-specific early arrival and effective-dated routing phases under one paycheck stream, projected raises/bonuses, bills, transfers, loans, committed refinances, receivables, assets, scenarios, and reconciliation.
- The Refinance planner supports one or multiple payoff loans, date-specific payoff projection, separate closing/payoff/first-payment dates, financed versus cash-paid fees, principal cash contribution, excess proceeds to a selected account, explicit commitment, immutable offer snapshots, automatic effective-dated secured-asset relinking, durable history, pre-close reverse cancellation, and chronological refinancing of a prior replacement loan.
- Committed refinance plans flow through the native daily ledger and effective liability set: only real closing cash and proceeds touch bank accounts, old payments stop on payoff, replacement payments begin on their selected date, secured assets follow a continuous stacked collateral chain, and cash lows, Spending Power inputs, and net worth update from the same records.
- Financial Records provides a canonical advanced library, provenance, forecast impact, related records, and direct editing without maintaining a separate calculation model.
- Every substantive loading surface now uses a theme-aware rounded shimmer skeleton with a reduced-motion fallback and accessible live status. Cash-changing and destructive forms use immediate reentrancy locks, setup exit flushes the latest draft, stale scenarios cannot be saved, long-list editors reveal and focus their form, and backup/restore/import/export/reset actions cannot overlap.
- Expected and protected forecasts roll from the current financial date. Transfers retain consolidated ownership while in transit, and recommendations reserve source capacity jointly.
- Cards preserve coming-due statements, open-cycle spending, future baseline assumptions, older history, payment policy, reported total/carrying snapshots, and explicit incomplete-timing states as distinct records. Cards, charge cards, and lines of credit report statement due, current balance, available credit, and balance carrying independently; paid-in-full history remains visible while carrying stays zero.
- Revolving accounts support effective-dated retirement/reactivation. Closure blocks purchases, recurring activity, new cycle openings, baselines, Spending Power, and advisor use from the selected date while retaining final earlier cycles, debt, later payoff cash, and history.
- Installment loans accept complete or partial lender facts, label calculated values, and support monthly/biweekly fully amortizing and balloon/bullet structures. Dated schedules separate total bank draft from debt service, expose interest/principal/remaining balance, distinguish contractual maturity from modeled payoff, and carry regular-draft overrides and additional principal through cash, liability, refinance, and net-worth outputs.
- Portable backup V2 encrypts a profile's managed financial state, settings, audit, debt metadata, refinance plans, immutable offer snapshots, payoff/collateral lineage, receivable timing anchors, and import lineage while leaving destination identity and sign-in password local. Restore validates the envelope and record graph, creates an encrypted safety copy, rejects ownership conflicts, and replaces data transactionally. Migrations through schema 21 repair validated legacy refinance lineage; schemas 22 through 26 add debt metadata, loan-payment treatment, revolving-account lifecycle, installment amortization structure, and optional bill-relative receivable timing with legacy-safe defaults. Schema 27 adds durable occurrence identity and target metadata so historical partial receipts remain stable through later amount, destination, recurrence-day, bill-anchor, cancellation, and restore changes.
- JSON/CSV export is explicitly plaintext, collision-free, formula-escaped, and separate from portable recovery.
- The Electron shell defaults to dark mode and uses context isolation, renderer sandboxing, a narrow typed bridge, validated IPC, restrictive navigation/permission behavior, custom local protocol, CSP, hardened fuses, and single-instance application identity.
- Automated engine, domain, database, contract, migration, backup, renderer, accessibility, and packaged-application checks are part of the `pnpm verify` baseline. The `dff8701` product baseline passed 425 synthetic unit and integration tests across 41 files, covering revolving-debt reconciliation, paid-in-full carrying, account closure, partial loan solving, balloon/bullet structures, additional-principal semantics, dated and bill-relative receivables, durable occurrence history, restore abuse cases, mutation locks, loading accessibility, refinance persistence, backup contracts, and dependent page behavior. The complete current-tree `pnpm verify` pipeline is not claimed because the restricted environment blocked the full Playwright Electron journey.

## V1 distribution foundation

- Source package metadata and release helpers identify version 1.1.0, Balance Book contributors, and the MIT License with exact dependency pins. A new tag has not been created.
- Electron Forge builds a self-contained Windows x64 Squirrel installer and portable archive while retaining the compatible internal identity `balance_book_mvp`.
- Forge stages the project MIT License and third-party notices alongside Electron/Chromium notices, and the release validator requires the project notices. Their presence was confirmed in the packaged 1.1 application; presence after installation on a fresh disposable profile remains an artifact-lifecycle gate.
- The maintained release command keeps strict signed public-production output separate from an explicitly labeled local unsigned-candidate lane. An offline unsigned 1.1 Squirrel setup and native uninstall helper were built for owner testing. A local three-file candidate also contains a previously created encrypted backup; because its backup password was unavailable during this run, its decrypted contents and restore behavior were not freshly verified and it is not evidence for the current backup gate.
- Published Status now has repeatable initial-build and later-sync clean-history mirror tooling, protected source and generic public privacy checks, a signed seven-file software-only public assembler, and a live fail-closed checker for private/public parity, one sanitized root, all advertised branch/tag/pull-request refs, commit/tag/ref metadata, private-history intersection, release/tag/source identity, fresh GitHub downloads, exact asset allowlisting, hashes, versions, expected publisher, packaged-to-installed executable identity, and legal-notice equality. The checker is necessary but does not replace the recorded dependency/license and disposable-profile lifecycle reviews.
- The exact current lock file passed `pnpm audit --prod` with no known vulnerabilities on 2026-07-16. Its 245-entry production license inventory and the sole metadata ambiguity are recorded in [DEPENDENCY_REVIEW.md](DEPENDENCY_REVIEW.md); any lock-file change requires a fresh review.
- Normal uninstall removes application files and shortcuts but preserves `%APPDATA%\Balance Book`; reinstall under the same Windows account retains local data and sign-in verification.
- A native uninstall helper confirms preserved data before invoking Squirrel uninstall.
- Signing configuration accepts external certificate inputs, but a real publisher certificate and valid public signatures have not been asserted.
- GitHub Actions use least-privilege read access and reviewed immutable action revisions; Dependabot retains npm and Actions update coverage.
- Public governance, installation, backup, threat-model, support, release, and supported-version documents are present.

## Current 1.1 validation status

The current 1.1 evidence is sufficient for continued local owner testing, not for public distribution.

- The `dff8701` product baseline passed 425 synthetic unit and integration tests across 41 files, including the solver, dated amortization, debt metadata persistence, cash-versus-debt payment semantics, revolving-debt summary, card closure, future-cycle boundaries, dated and bill-relative receivable effects, split/partial/cancelled receipt history, recurrence and anchor edits, adversarial restore validation, UI reentrancy guards, shimmer accessibility, refinance effects, backup contracts, and dependent page contracts. The current intended public tree passes 463 synthetic tests across 43 files. The ignored private validation harness remains separately gated on its explicitly isolated data-directory inputs and is not part of the publish tree.
- The packaged 1.1 Electron application passed an isolated smoke check with the production native SQLite module. The full Playwright input-to-output journey could not run because process creation is blocked in the restricted sandbox; no current 1.1 Playwright, responsive-layout, theme-accessibility, or complete UI journey pass is claimed.
- An offline, explicitly unsigned Windows x64 Squirrel 1.1.0 setup and native uninstall helper were built. This proves artifact construction, not signing, fresh-profile installation, upgrade, uninstall/reinstall, or restore behavior.
- The local candidate metadata records its exact clean source commit, Setup SHA-256, and worktree state, and its packaged native SQLite runtime matches the separately verified Electron 43 binary.
- An ignored private migration utility ran the receivable timing conversion against an integrity-checked pre-1.1 database snapshot in dry-run, first-apply, and no-op repeat-apply modes. Application-native validation confirmed schema 26, unique dated cash receipts, one intended destination account, unchanged unrelated records, no ambiguous duplicate cash events, exact account and consolidated daily deltas, and SQLite integrity `ok`; the live user database was not mutated from the restricted build environment.
- An ignored private oracle validation used application-native mechanics against an isolated populated profile and confirmed declining active installment balances, zero modeled contractual balloons for fully amortizing loans, zero revolving carry under paid-in-full evidence, preserved statement history, and exact native fixture parity. Current tracked fixtures use unrelated synthetic institutions, account identifiers, and amounts; the private database and workbook remain ignored.
- The final 173-file reviewable working tree passed the generic scanner, the separately protected private-pattern review, 37 privacy path/extension regressions, and an independent search for known private names, institutions, workbook names, and unique financial fingerprints. Tracked fixtures use unrelated synthetic institutions, identifiers, and amounts. Seventeen new synthetic tests cover exact file-list paths, shallow refusal, Unicode paths, protected and generic commit/tag/ref metadata, and missing pattern lists, but the restricted environment skipped them because Node cannot launch child processes. The existing 29-commit development history remains private and unsuitable for direct publication; the exact committed tree, clean mirror, live ref inventory, and fresh clone still require release-environment execution.
- The candidate folder's encrypted backup predates this validation pass. Without its backup password, this run did not decrypt, inspect, or restore it, so it must not be described as a freshly verified 1.1 backup.

## Release validation still required before public distribution

- Re-run the complete 1.1 source pipeline on the final committed tree in an unrestricted environment, including the full Playwright Electron journey and current-tree privacy checks; retain the 463-test intended-public-tree result and baseline package-smoke evidence as local pre-release results.
- Preserve the 2026-07-16 advisory and production-license results for the exact current lock file; rerun both if the lock file changes or publication is deferred.
- Sign and timestamp the installer, installed executable, and uninstaller with a real publisher certificate, then verify `Valid` Authenticode status. The local candidate is intentionally and visibly unsigned.
- Perform fresh-user install, first-run, restart, uninstall, preserved-data reinstall, clean-data restore, 1.0-to-1.1 and any other advertised upgrade, shortcut, taskbar, packaged-notice, and post-install debt/forecast/net-worth functional tests from the exact produced files on a normal Windows profile.
- Independently inspect and publish only a sanitized clean-history public mirror. Do not change the existing development repository to public.
- Enable a private vulnerability-reporting channel and publish only reviewed, signed, user-data-free artifacts.
- Restore working GitHub authentication/network access, synchronize the private default branch, create the intended `15decesaremj/balance-book` public mirror, and publish a signed stable release. The intended public URL and release page do not currently exist as verified install sources.

## Known limits

- The local sign-in password is an application privacy boundary; the live database is not encrypted at rest.
- V1 has no telemetry, bank sync, cloud service, remote access, auto-update, password recovery, or automatic money movement.
- Plaintext exports require the user to provide storage protection.
- A backup cannot be recovered without its backup password, and two independently active profiles are not merged automatically.
- Windows 11 x64 is the supported V1 desktop target; other operating systems and architectures are not claimed.
- No formal security, accessibility, financial-advice, or third-party legal certification has been completed.
